{% extends ('CoreBundle::layout.html.twig')%}

{% block body %}
<div class="container">
  <div class="row">
    <div class="col-lg-12">
      <a href="{{path('forums_home')}}">Retour à l'accueil</a>
      <h3 class="text-center">{{ sujet.titre }}</h3>
      <br>
      <h4 class="text-center">Par <strong>{{ sujet.auteur }}</strong>, le {{ sujet.dateCreation|date('d/m/Y') }}</h4>
      <hr />
      <p class="text-center">{{ sujet.contenu }}</p>
      <br>
      <br>
    </div>
    <div class="row">
      <h4 class="text-center">Commentaires liés</h4>
      <hr />
    </div>
  </div>
</div>
{% endblock %}

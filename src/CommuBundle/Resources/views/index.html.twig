{% extends ('CoreBundle::layout.html.twig')%}
{% block body %}
<div class="container">
  <div class="row">
    <div class="col-lg-12">
      <h2 class="text-center">La communauté, la fondation</h2>
      <hr />
      <p class="text-center">Amis de bon goût, voici l'heure de vous donner les clés de JVN-Network, vous êtes ici chez vous, libre,
      tout puissant et libéré de toute contraintes, vos propos ne seront pas modérés dans cette section (jusqu'à une certain seuil) et
    vous êtes libre de vaquer à vos occupations, que ce soit la vidéo, le tchat, la webcam ou les "LDS", tout est là pour vous, bonne journée</p>
    </div>
    <div class="col-lg-6">
      <!-- Bloc Twitter -->
      <h3 class="text-center">Twitter</h3>
      <hr />
    </div>
    <div class="col-lg-6">
      <!-- Bloc Tchat -->
      <h3 class="text-center">Le tchat</h3>
      <hr />
      {% if is_granted('IS_AUTHENTICATED_REMEMBERED')%}
      <div class="well">
        {% for tchat in tchat %}
          <h5 class="text-center">{{ tchat.auteur }}</h5>
          <p class="text-center">{{ tchat.contenu }}</p>
          <br>
          <hr />
        {% endfor %}
      </div>
      {% endif %}
    </div>
  </div>
  <div class="row">
    <div class="col-lg-6">
      <!-- Bloc Twitch -->
      <h3 class="text-center">La chaîne Twitch</h3>
      <hr />
    </div>
    <div class="col-lg-6">
      <!-- Bloc Webcam -->
      <h3 class="text-center">La Webcam</h3>
      <hr />
    </div>
  </div>
</div>
{% endblock %}
